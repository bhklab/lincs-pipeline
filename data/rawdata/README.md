# rawdata

Downloaded LINCS source files live here. The pipeline assumes source metadata under `rawdata/metadata/` and CLUE expression/metadata downloads under source-specific subdirectories.

Full LINCS expression inputs can require tens of GB. Files here are ignored by git.
